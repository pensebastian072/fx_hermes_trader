[project]
name = "fx-hermes-trader"
version = "0.1.0"
description = "Local-first, paper-trading-only FX research harness. No live execution."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
